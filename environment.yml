name: ansible-devenv
channels:
  - defaults
dependencies:
  - python=3
  - pip
  - pip:
      - ansible-compat
      - ansible-core
      - ansible-lint
      - argcomplete
      - molecule
      - molecule-podman
      - pre-commit
      - yamllint
